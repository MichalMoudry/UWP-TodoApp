<Page
    x:Class="TodoApp.Views.Pages.ListsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TodoApp.Views.Pages"
    xmlns:models="using:TodoApp.Models.Database"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView Margin="5" x:Name="lists">
        </ListView>

        <CommandBar Grid.Row="1">
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="Add" x:Name="addButton" Click="AppBarButton_Click" />
                <AppBarButton Icon="Sort" x:Name="sortButton" Click="AppBarButton_Click" />
                <AppBarButton Icon="Bullets" x:Name="selectButton" Click="AppBarButton_Click" />
            </CommandBar.PrimaryCommands>

            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Setting" x:Name="settingsButton" Click="SecondaryAppBarButton_Click" />
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Grid>
</Page>
